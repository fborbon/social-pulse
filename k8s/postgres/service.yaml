apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: social-pulse
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  clusterIP: None    # Headless service — pods connect by DNS name postgres-0.postgres
